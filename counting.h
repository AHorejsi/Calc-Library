#ifndef COUNTING_H
#define COUNTING_H

#include <stdint.h>


uint64_t factorial(const uint64_t);

uint64_t choose(const uint64_t, const uint64_t);

uint64_t permute(const uint64_t, const uint64_t);

uint64_t choose_with_replacement(const uint64_t, const uint64_t);


#endif
